# react-exp9-controlled-registration-form
To build a **user registration form** using **controlled components** in React.   Each input field’s value is managed by React state, ensuring synchronization between UI and internal data.
